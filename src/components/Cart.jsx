export default function Cart() {
    return (
        // TODO: Create cart modal component, that is shown when the user clicks on the cart icon
        <div>
            <h2>Cart</h2>
            <ul>
                {/* TODO: Show cart items */}
            </ul>
        </div>
    );
}